{% extends 'base.html.twig' %}

{% block title %}Modifier l'image{% endblock %}

{% block body %}

  <div class="center col-10 mt-0 d-flex flex-column justify-content-between">
      <h1>Modifier l'image</h1>

      {{ include('image/_form.html.twig', {'button_label': 'Modifier'}) }}

      {{ include('image/_delete_form.html.twig') }}
      
      <a href="{{ path('image_index') }}">Retour au menu précédent</a>
  </div>

{% endblock %}
