<form method="post" action="{{ path('user_delete', {'id': user.id}) }}" class="d-flex justify-content-center" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet utilisateur ?');">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
  <button class="btn btn-danger my-2 text-center w-auto">Supprimer</button>
</form>
