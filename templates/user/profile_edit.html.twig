{% extends 'base.html.twig' %}

{% block title %}Modifier vos informations{% endblock %}

{% block body %}
  <div class="center col-10 mt-0 d-flex flex-column justify-content-between">
    <h1>Modifier vos informations</h1>

    {{ include('profile/_form.html.twig', {'button_label': 'Modifier'}) }}

    {{ include('user/_delete_form.html.twig') }}

    <a href="{{ path('user_profile') }}">Retour à mon profile</a>
  </div>
{% endblock %}
