{% extends 'base.html.twig' %}

{% block title %}Page de contact{% endblock %}

{% block body %}

  <div class="center mt-0 d-flex flex-column justify-content-between">
    <div class="row">

      <h1>Page de contact</h1>

      {{ form_start(form)}}
      {{ form_widget(form) }}
      <button class="btn btn-dark mt-3">Envoyer</button>
      {{ form_end(form) }}

    </div>
  </div>

{% endblock %}
