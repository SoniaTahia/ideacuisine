{% extends 'base.html.twig' %}

{% block title %}Nous contacter{% endblock %}

{% block body %}

  <div class="center col-10 mt-0 d-flex flex-column justify-content-between w-100">
    <div class="row cat d-flex justify-content-center m-0">
      <h1 class="slogan mt-3 mb-5 mx-0 d-flex justify-content-center fw-lighter fst-italic">Nous contacter</h1>

      <div class="w-50 h-75">
        {{ form_start(form)}}
        {{ form_widget(form) }}
        <div class="d-flex justify-content-center">
          <button class="btn btn-dark mt-3 w-50">Envoyer</button>
        </div>
        {{ form_end(form) }}
      </div>
    </div>
  </div>

{% endblock %}
